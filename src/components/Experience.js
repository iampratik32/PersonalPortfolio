import React from 'react'
import { MainContainer } from '../assets/styles'
import ContainerTitle from './ContainerTitle'

function Experience({ eRef }) {
    return (
        <MainContainer>
            <ContainerTitle title={'Experience'} pRef={eRef} />
            asd
        </MainContainer>
    )
}

export default Experience   